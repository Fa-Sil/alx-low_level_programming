#include "main.h"
/**
 * int _strlen - returns length of the string
 * @s: parameter String
 * Return: returns length integer
 */
int _strlen(char *s)
{
	size_t length = 0;

	while (*str++)
		length++;

	return (length);
}
