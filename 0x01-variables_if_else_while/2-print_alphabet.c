#include <stdlib.h>
#include <time.h>
#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	char l = a;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	while (l <= z)
	{
		putchar(l);
		putchar('\n');
		++l;
	}
	return (0);
}
